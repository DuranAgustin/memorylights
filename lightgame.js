var comparray = [];
var userarray = [];
var usercorrect = true;
var counter = 0;

// random number generated
// assign number with row/colum
// 1-top left 2-topright random number makes then adds corrisponding position
// while usercorrect == true do the following:
// img light up for a .5 a second at location then stops
// if the user clicks on a img it will add that value to the user array
// the checks if both arrays are correct if so add a new random array to the
// comp array and re-display location else it will return an empty array in which
// it will start again at round 1 with 1 light being on and then added to the array.
